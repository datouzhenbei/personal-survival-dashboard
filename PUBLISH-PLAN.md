# 对外发布方案（PUBLISH-PLAN）

> 适用范围：个人生存看板（`personal-survival-dashboard`）
> 编制：项目负责人（AI） · 日期：2026-09-25 · 状态：待用户决策

---

## 0. 先纠正三个前提（省钱的三个结论）

**① 你的站其实已经对外发布了。**
现在任何拿到链接的人都能打开 `https://datouzhenbei.github.io/personal-survival-dashboard/`，无需登录、无区域限制。所以真正的问题不是"怎么发布"，而是：

- 要不要换成一个**自己的网址**？
- 要不要让**国内访问变快**？
- 要不要为了**跨设备同步**上后端？

**② 纯静态站不需要服务器。**
本站无数据库、无写入接口、无用户上传、无后台程序，所有数据都在用户自己的浏览器 `localStorage` 里。因此传统那套"服务器配置"——CPU 核数、内存、系统盘、带宽峰值、并发连接数——**一条都用不上**。你真正需要的是三件东西：

```
静态文件托管  +  域名  +  HTTPS 证书
```

（其中托管已经有了，HTTPS 是免费的，唯一要花钱的是域名。）

**③ 你永远不会因为流量而花钱。**
实测首屏需加载 11 个文件，合计 **126.8 KB**（gzip 后约 38 KB）：

| 文件 | 原始大小 |
|---|---|
| index.html | 4,655 B |
| css/style.css | 28,216 B |
| js/core/*（5 个） | 30,362 B |
| js/ui/*（4 个） | 66,636 B |
| **合计** | **129,869 B（126.8 KB）** |

| 月访问量 | 月流量 | 落在哪 |
|---|---|---|
| 1,000 次 | ~0.13 GB | 免费额度白送 |
| 10,000 次 | ~1.3 GB | 免费额度白送 |
| 100,000 次 | ~13 GB | 仍远低于 GitHub Pages 100 GB/月 与 Cloudflare 无限额度 |

结论：**这个站的成本结构里，服务器和流量都可以视为 ¥0**，唯一变量是域名和"要不要国内加速"。

---

## 1. 需要什么：按优先级分三档

### A. 必需（缺了别人打不开 / 用不了）

| 项目 | 现状 | 说明 |
|---|---|---|
| 静态文件托管 | ✅ 已有（GitHub Pages） | 免费、自动 HTTPS |
| HTTPS 证书 | ✅ 已有（自动签发） | 免费，无需手动续期 |
| 可访问的入口链接 | ✅ 已有 | `github.io` 二级域名 |

结论：**这三项已经齐了，你现在就具备"对外发布"的全部硬条件。**

### B. 强烈建议（让"对外"这件事真的成立）

| 项目 | 为什么需要 | 现状 |
|---|---|---|
| **自有域名** | `github.io/xxx` 又长又不像自己的东西；有域名才能随时换托管商而不换网址 | ❌ 缺 |
| **分享卡片（OG 标签）** | 微信/QQ/微博里粘贴链接时，会显示标题+简介+缩略图，而不是一条白条 | ❌ 缺 |
| **favicon / 手机主屏图标** | 浏览器标签页、添加到主屏后的图标 | ❌ 缺 |
| **robots.txt + sitemap.xml** | 让搜索引擎能正确收录（不做也行，做了别人搜得到） | ❌ 缺 |
| **404 页面** | 打错地址时不是 GitHub 的丑页面 | ❌ 缺 |
| 页脚备案号 | **仅在国内节点托管时才需要** | 视方案 |

### C. 可选 / 进阶

| 项目 | 价值 |
|---|---|
| **PWA（manifest + Service Worker）** | 可"添加到手机主屏"、断网也能打开。**和"数据全在本地"的定位天然契合**，性价比很高 |
| 访问统计 | 想知道有没有人在用。**务必选无 Cookie 方案**（Cloudflare Web Analytics / Umami 自托管），否则要加 Cookie 告知 |
| 自动部署（GitHub Actions） | 目前 push 后 Pages 还要手动触发重建，可以自动化掉 |
| 自定义邮箱/域名邮箱 | 对外联络用（可选） |

### D. 只有加后端才需要（**现在不需要**）

一旦要做"跨设备同步""账号登录""云端备份""多人共享"，就会引入一整套额外成本与义务：

- 云服务器 + 数据库 + 备份策略（月 ¥10–100 起，且需要运维）
- **ICP 备案变成强制**（境内服务器）
- **隐私政策 + 个人信息保护合规**（收集了用户账号/邮箱即为"个人信息处理者"，有告知、删除、泄露报告等义务）
- 安全防护（登录、防刷、防注入、限流）

**触发条件很明确：只有当你需要"换台电脑还能看到同一份数据"时才值得做。** 目前的 V1.1 已经用"导出/导入备份 + 备份提醒"覆盖了 80% 的需求，成本差着一个数量级。

---

## 2. 托管方案对比（5 档）

| 方案 | 组成 | 年成本 | 国内访问 | 需备案 | 适合谁 |
|---|---|---|---|---|---|
| **A. 保持现状** | `github.io` 二级域名 + GitHub Pages | **¥0** | 慢，偶发打不开 | 不需要 | 自己用 + 发给朋友看 |
| **B. 只买域名** | 自有域名 → CNAME 到 GitHub Pages | **¥85–95**（.com）<br>¥33–42（.cn） | 同 A | 不需要 | 想要个好网址，其他不动 |
| **C. 域名 + Cloudflare Pages** | 自有域名 + CF 全球 CDN 托管 | **¥85–95** | 一般偏可用 | 不需要 | ✅ **推荐起步**：无限流量、免备案、可随时换托管 |
| **D. 域名 + 国内边缘加速** | 腾讯云 EdgeOne（国内节点） | **¥360+/年**（个人版约 ¥30/月，有免费版 50 GB/月） | **最快**（TTFB ≈55 ms） | **必须备案** | 面向国内公众、要求速度 |
| **E. 域名 + 对象存储 + CDN** | 腾讯云 COS / 阿里云 OSS + CDN | **¥50–300/年**（按量） | 快 | **必须备案** | 流量稳定后按量更省钱 |

> 提示：D/E 之所以要备案，不是平台要求，而是**法律要求**——只要网站部署在中国大陆境内的节点上对外提供服务，就必须先完成 ICP 备案。

**不建议的做法**：为了这个站去买云服务器（轻量应用服务器 2核2G 约 ¥100–300/年）。纯静态站用它属于典型"性能过剩"，还要自己管证书续期、系统补丁、安全防护——花钱买麻烦。

---

## 3. 成本清单

| 项目 | 费用 | 备注 |
|---|---|---|
| 域名 `.com` | 首年 ¥81–85，续费 ¥86–95/年 | 首年与续费价差最小 |
| 域名 `.cn` | 首年 ¥25–38，续费 ¥33–42/年 | 备案流程更顺，价格更稳 |
| 静态托管（A/B/C 方案） | **¥0** | GitHub Pages / Cloudflare Pages 免费额度远超需求 |
| 静态托管（D 方案） | ¥30/月（有免费版 50 GB/月） | 国内节点 |
| 静态托管（E 方案） | 按量，存储+流量+请求三项叠加 | 小流量约几十元/年 |
| HTTPS 证书 | **¥0** | 平台自动签发（Let's Encrypt 系） |
| ICP 备案 | **¥0**（自办） | 时间成本 10–22 个工作日 |
| **合计（推荐路径 C）** | **约 ¥85–95/年** | 相当于每月 7 块钱 |
| 合计（面向国内方案 D） | 约 ¥450/年 + 2–3 周备案等待 | |

---

## 4. 域名怎么选

- **后缀**：优先 `.com`（国际通用、续费稳）；其次 `.cn`（备案友好、便宜）。避开首年 1 元、续费 60+ 的引流后缀（`.top` `.xyz` `.icu`）。
- **名字**：短、好念、能记住。本站可考虑 `survival-dashboard.com` 一类；若含个人品牌（如 `datouzhenbei.com`）也可。
- **注意**：**国内注册商注册的域名需先完成实名认证**（1–3 个工作日），备案前必须先实名，且实名信息须与备案主体一致。
- **务必开启自动续费**：域名过期进入赎回期，赎回费可高达几百到上千元，且网址会直接失效。

### 4.5 买完域名怎么接上去（含一个必踩的坑）

拿到域名后有两条路：

| | 路由一：只改解析 | 路由二：迁到 Cloudflare Pages（推荐） |
|---|---|---|
| 做法 | 域名 CNAME 指向 `datouzhenbei.github.io` | 仓库接入 Cloudflare Pages，自定义域名指向 Pages |
| 耗时 | 约 5 分钟 | 约 30 分钟 |
| 国内速度 | 与 `github.io` 相同（波动大） | 明显改善（但仍无大陆节点） |
| 后续维护 | 无 | 需自己维护构建配置 |

**🔴 硬约束（路由二必踩）**：Cloudflare Pages 绑定**根域名**（`example.com`，不带 `www`）要求该域名已在 Cloudflare 托管 DNS → **必须先到注册商处把 NS（名称服务器）改成 Cloudflare 给的两个地址**。若 DNS 留在原注册商、只加一条 CNAME，则**只能绑子域名**（`www.example.com`、`kanban.example.com`）。

改 NS 只是把解析权交给 Cloudflare，**不是域名转移**，注册商与域名所有权都不变。

**四步上线（路由二）**：

1. 注册域名 → 完成实名认证（1–3 个工作日）
2. Cloudflare 添加站点 → 拿到 2 个 NS 地址 → 到注册商处改 NS（几分钟到几小时生效）
3. Cloudflare Pages → 连接 GitHub 仓库 `datouzhenbei/personal-survival-dashboard` → 框架预设选 **None**、构建命令**留空**、输出目录 `/` → Deploy
4. Pages 项目 → Custom domains → 添加 `example.com` 与 `www.example.com` → 自动签发 HTTPS 证书（几分钟）

**Cloudflare Pages 免费额度 vs 本站实际占用**：

| 项 | 免费额度 | 本站占用 |
|---|---|---|
| 带宽 | **无限** | 首屏约 127 KB |
| 站点数 | 100 个项目 | 1 |
| 单站文件数 | 20,000 | 约 15 |
| 单文件大小 | 25 MiB | 最大约 24 KB |
| 自定义域名 | 100 个 | 1–2 |
| HTTPS 证书 | 免费自动签发 | — |
| 构建次数 | 500/月 | 极低 |

**重要预期管理**：Cloudflare 免费版**没有中国大陆节点**（大陆节点只对企业版开放）。绑自定义域名后国内访问会比 `pages.dev` / `github.io` 明显改善，但**不要指望国内秒开**。将来真觉得慢，可在前面加一层国内 CDN（腾讯云 EdgeOne 等）—— 那时才可能涉及备案/实名，**域名不用换，只改解析**。

**付款备注**：Cloudflare 免费版**无需信用卡**；若想直接在 Cloudflare Registrar 买域名，需国际信用卡/PayPal。国内用微信/支付宝在腾讯云/阿里云买域名、再把 NS 改到 Cloudflare，流程更省事。

---

## 5. ICP 备案（只在方案 D/E 时需要）

**触发条件**：网站使用**中国大陆境内节点**对外提供服务。用境外节点（含中国香港）不需要备案，但国内访问速度会明显打折。

**个人备案需要准备**：

1. 已完成**实名认证**的域名（1–3 个工作日）
2. **身份证**正反面照片 + 手持身份证照片（现在多数走人脸识别，已取消幕布照）
3. **境内服务器/托管资源**（部分省份要求购买时长 ≥3 个月）
4. 备案联系人的手机号与邮箱
5. 部分省份额外要求居住证/暂住证等

**流程与时长**：

| 步骤 | 时长 |
|---|---|
| 服务商（阿里云/腾讯云）初审 | 1–2 个工作日 |
| 工信部短信核验 | 初审通过后收到短信，需 **24 小时内**完成 |
| 通信管理局审核 | **5–20 个工作日**（个别省份电子化后缩短到 5 个工作日） |
| **合计** | **约 10–22 个工作日** |

**上线后的持续义务**：

- 网站首页底部悬挂**备案号**，并链接到工信部备案查询网站
- 备案成功后 **30 日内**办理**公安联网备案**
- 网站实际内容须与备案填报信息一致（不符会被驳回/注销）

**⚠️ 金融类前置审批**：备案规则中明确列出**金融类**等行业需在备案前完成**前置审批**。也就是说，如果这个站未来要加入任何实质性的投资/理财内容，备案环节会多一道硬门槛。

---

## 6. 合规红线（三条，必须守住）

| 情形 | 要求 |
|---|---|
| **现状**：纯个人工具，不含任何投资建议 | ✅ 合规。页脚已有免责声明，数据不上传，无个人信息收集 |
| **一旦加入投资/财务建议** | ❌ 需要**证券投资咨询业务资格**，并办理备案金融类前置审批。这条路个人基本走不通——建议永久保持"仅计算/展示工具"定位 |
| **加入访问统计 / 广告 / 收费** | 统计需告知（选无 Cookie 方案可规避）；广告与收费可能涉及**经营性 ICP 许可**，个人性质不适用 |

**一句话原则**：这个站越"工具化、纯本地、不赚一分钱"，合规成本越低。保持现状是最优解。

---

## 7. 推荐路径

**推荐：方案 C（自有域名 + Cloudflare Pages），约 ¥90/年，零备案。**

理由：

1. 把网址变成自己的（长期资产），随时能换托管商而网址不变；
2. 无限流量、免费 HTTPS、自动构建部署；
3. 完全不用备案，今天买域名今天就能上线；
4. 将来真觉得国内慢，再走 D 方案（备案 + 腾讯云 EdgeOne）——**域名不用换，只换解析**；
5. 反过来先备案再想域名，会白等 2–3 周。

**不要为了"以后可能要"提前备案。** 备案绑定的是境内的接入资源，等真有国内用户抱怨速度时再办，时间窗口完全来得及。

**分场景速查**：

| 你的情况 | 选 |
|---|---|
| 就自己用 + 发朋友 | A（现状，¥0） |
| 想要个好网址 | B 或 C（¥90/年） |
| 要给国内一群人用、要快 | D（备案 + ¥450/年） |
| 想换电脑看同一份数据 | 另立项目：上后端（成本 ×10，且需备案+隐私合规） |

---

## 8. "发布增强包"（可选，我可直接实施）

如果决定走对外发布，建议顺手补上这 7 项，**纯前端、不改任何业务逻辑**：

| # | 项目 | 说明 |
|---|---|---|
| 1 | `favicon.ico` + `apple-touch-icon.png` | 标签页与手机主屏图标 |
| 2 | OG / Twitter 分享标签 | 微信、QQ 转发时显示标题+简介+缩略图 |
| 3 | `robots.txt` | 允许收录 |
| 4 | `sitemap.xml` | 提交搜索引擎 |
| 5 | `404.html` | 自定义错误页 |
| 6 | PWA（manifest + SW） | 可"添加到主屏"、断网可用；**与本地数据定位极契合** |
| 7 | `<meta name="theme-color">` | 手机浏览器地址栏配色，视觉更整 |

配套还要：换域名后更新 README 与页脚仓库链接、`<link rel="canonical">` 指向正式域名。

---

## 9. 上线检查清单（走对外发布时逐条过）

- [ ] 域名注册 + 实名认证完成
- [ ] DNS 解析/托管绑定完成（CNAME 或 A 记录）
- [ ] HTTPS 生效、`http://` 自动跳 `https://`
- [ ] 手机 + 桌面各验证一次真实渲染（非无头）
- [ ] favicon、分享卡片、404 页生效
- [ ] 页脚免责声明仍在、无投资建议内容
- [ ] 若备案：备案号已挂页脚并链接工信部，30 日内完成公安联网备案
- [ ] 域名已开启自动续费
- [ ] 备份/恢复流程可用（V1.1 已具备）
- [ ] 确认仍是**零网络请求**（打开开发者工具 Network 面板核对）

---

## 10. 需要用户拍板的 3 件事

| # | 问题 | 备选 |
|---|---|---|
| **P1** | 要不要买自有域名？ | 买（推荐）/ 暂不买，继续用 `github.io` |
| **P2** | 主要给谁用？ | 自己+朋友（→ 方案 A/B/C，免备案）/ 国内公众且要求速度（→ 方案 D，需备案） |
| **P3** | 要不要现在做"发布增强包"（favicon/分享卡片/PWA/robots/sitemap/404）？ | 做（我可直接实施）/ 先只做分享卡片和 favicon / 暂不做 |

**关于 P1 的一个提醒**：域名注册需要实名认证，且要绑定你的手机号与身份证信息，属于**你的个人资产决策**，我不代为注册。你把想好的域名告诉我，我可以帮你核查可用性、比对注册商价格，并给出解析配置的具体步骤。
