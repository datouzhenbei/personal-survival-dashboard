# V1.1 实现方案：数据更耐用（Data Durability）

- 提出人：项目负责人（小陆）
- 日期：2026-09-25
- 基线版本：`v1.0.0`（commit `aa971b9`）
- 目标版本：`v1.1.0`
- 约束：**纯前端 / 零后端 / 零网络请求 / 不涉及投资建议**（不破坏 V1 边界）

---

## 一、要解决的问题

V1 的所有数据存在浏览器 `localStorage`（前缀 `psd_`）。当前存在 4 个真实风险：

| # | 风险 | 现状 | 后果 |
|---|---|---|---|
| R-1 | **数据只在单浏览器里** | 无备份机制、无任何提醒 | 清缓存 / 换设备 / 重装浏览器 → 数据全丢且用户不自知 |
| R-2 | **导出文件名固定** | 每次导出都叫同一个名字 | 第二次导出覆盖第一次，无法保留历史快照 |
| R-3 | **导出文件无格式标识** | 只 dump 裸结构 | 文件看不出是什么、什么时候导的；将来改结构无法识别旧文件 |
| R-4 | **导入无校验** | 直接解析后写入 | 拿到错文件 / 损坏文件会静默失败或写脏数据 |

---

## 二、目标（本次范围）

- **G1 导出可识别**：文件名带时间戳；文件内含格式标记、schema 版本、导出时间
- **G2 导入必校验**：结构 + 版本双重校验，不兼容时**明确告诉你原因**，不再静默失败
- **G3 导入前可见**：先弹「导入预览」，列清楚"这份备份里有什么、会覆盖什么"，你确认后才写入
- **G4 该备份时提醒**：距离上次导出过久或数据有变动未备份时，页面内出现**温和提示条**（可「稍后再说」）
- **G5 有数据安全区**：页面上有一块地方能一眼看到「当前数据量 / 最后备份时间 / 一键导出」

## 三、非目标（明确不做，避免范围蔓延）

- ❌ 不做云端同步、不做账号体系（保持零后端）
- ❌ 不做自动定时导出（浏览器无法在无用户操作时写文件，做不到就是做不到）
- ❌ 不做加密（无密码体系；如确有需要，留到 V1.2 单独评估）
- ❌ 不改动 V1 已有的核心计算逻辑（`life.js` / `savings.js` 算法不动）

---

## 四、技术方案

### 4.1 导出格式升级（新增信封）

当前 `Storage.exportJSON()` 导出的是内部结构 `{__v, __t, data}`。改为带元信息的信封：

```json
{
  "psdExport": true,
  "formatVersion": 1,
  "appVersion": "1.1.0",
  "schemaVersion": 1,
  "exportedAt": "2026-09-25T05:50:00.000Z",
  "itemCount": 5,
  "data": { ... }
}
```

**向后兼容**：导入时若检测到**没有** `psdExport` 标记但**有** `__v` 字段 → 判定为「V1 旧版备份」，走旧解析路径，并提示"已识别为 V1 旧版备份，已成功导入"。

### 4.2 导出文件名

`psd-backup-YYYYMMDD-HHmmss.json`（本地时间，例如 `psd-backup-20260925-135000.json`）。
→ 连续导出不再互相覆盖，天然形成时间线快照。

### 4.3 导入校验规则（按顺序）

1. 必须是合法 JSON（否则：「文件不是有效的 JSON」）
2. 必须有 `psdExport:true` **或** 旧版 `__v`（否则：「这不是本应用的备份文件」）
3. `schemaVersion` 必须 ≤ 当前支持的最大版本
   （若更高：「此备份由更新版本生成，请先升级页面后再导入」）
4. `data` 必须是对象，且至少含 1 个已知数据项（否则：「备份内容为空或结构异常」）
5. 逐字段类型与范围校验（数值须为有限数字且在 `LIMITS` 内）→ 越界字段**单独警告**并按默认值处理，不整份拒绝（更友好，且不丢其他可用数据）
6. 文件体积 > 5MB → 提示「文件异常偏大，可能不是本应用的备份」

所有校验在**写入之前**完成，**校验不通过绝不改动现有数据**。

### 4.4 导入流程（两步确认）

```
选择文件
  → 解析 + 校验（不动数据）
  → 【导入预览对话框】
       · 文件名 / 导出时间 / 数据项清单
       · 与当前数据的差异：哪些项将被覆盖
       · 校验警告（若有）
  → 你点「确认导入」
  → 先把当前数据暂存内存 → 写入 → 成功则 Toast；失败则回滚并报错
  → 刷新页面状态
```

### 4.5 备份提醒机制

- localStorage 新增两个时间戳：`psd_lastExportAt`（最后导出时间）、`psd_dataChangedAt`（最后数据变更时间）
- 提示条出现条件（满足任一）：
  - 从未导出过，且已有数据
  - `dataChangedAt > lastExportAt`（有改动未备份）
  - 距 `lastExportAt` 超过阈值天数（默认 **14 天**）
- 交互：提示条**不弹窗、不阻塞**操作；点「稍后再说」后 24 小时内不再出现

### 4.6 页面结构（数据安全区）

在现有「数据管理」区**上方**新增一张卡片：

```
┌─ 数据安全 ─────────────────────────────┐
│  当前数据 5 项 · 最后备份 3 天前        │
│  [ 导出备份 ]  [ 导入备份 ]             │
│  ⚠ 检测到有改动尚未备份   [稍后再说]    │  ← 条件出现
└────────────────────────────────────────┘
```

### 4.7 涉及文件与改动点

| 文件 | 改动内容 | 归属 |
|---|---|---|
| `js/core/config.js` | 新增 KEY（lastExportAt / dataChangedAt）、导出格式常量、APP_VERSION | 我 |
| `js/core/storage.js` | `exportJSON` 改新信封；新增 `buildExport()`、`parseImport(raw)`、`migrateLegacy()` | 我 |
| `js/core/index.js` | 导出新增 API | 我 |
| `js/ui/render.js` | 数据安全卡片、导入预览对话框、备份提示条 | 我 / Trae |
| `js/ui/app.js` | 导入两步流程编排、导出后写 `lastExportAt`、提示条状态管理 | 我 / Trae |
| `js/ui/components.js` | 如需新增 Banner / DiffTable 组件 | 我 / Trae |
| `css/style.css` | 新组件样式 + 三档响应式 | 我 / Trae |
| `README.md`、`UI-REQUIREMENTS.md` | 同步文档 | 我 |

**`life.js` / `savings.js` 不改动**——本次不动任何计算逻辑。

---

## 五、验收标准（11 条，全部可复现）

1. 导出文件名带时间戳；**连续导出两次不覆盖**
2. 导出内容含 `psdExport` / `schemaVersion` / `exportedAt`
3. 用 V1 **旧格式**文件导入 → 成功，且提示「已识别为 V1 旧版备份」
4. 导入**损坏 JSON** → 明确报错，现有数据**零改动**
5. 导入 `schemaVersion` **更高**的文件 → 拒绝并说明原因
6. 导入预览列出的数据项与文件实际内容一致
7. 确认导入 → 刷新页面后数据正确
8. 备份提醒：把 `lastExportAt` 改成 30 天前 → 出现提示条；点「稍后再说」→ 消失
9. 五档视口（1440 / 1024 / 820 / 390 / 360）**逐元素**检测横向溢出 = 0
10. **零网络请求**（Network 面板无新增请求）
11. console 零报错；`localStorage` 不可用时给出降级提示而非白屏

---

## 六、风险与对策

| # | 风险 | 对策 |
|---|---|---|
| RK-1 | 隐私模式 / 受限环境 `localStorage` 不可用 | 启动探测 + 降级提示；已有 V1 的 try/catch 兜底 |
| RK-2 | 旧格式判定误伤新文件 | 用**显式标记**区分（`psdExport`），不靠字段猜测 |
| RK-3 | 导出文件含个人参数（预期寿命、存款） | 纯本地下载、无外传；在预览与提示中明确写「该文件含你的个人参数，请妥善保管」 |
| RK-4 | `schemaVersion` 将来变更 | 本次先把版本号体系立起来，V1.2 起就有依据 |

---

## 七、实现路径

| 阶段 | 内容 | 交付 |
|---|---|---|
| S1 | core 层：格式 + 校验 + 迁移 | 可独立自测的纯函数 |
| S2 | UI 层：安全卡片 + 预览对话框 + 提示条 | 可交互页面 |
| S3 | 验收：11 条用例 + 五视口回归 | 验收记录 |
| S4 | 上线：`scripts/push.ps1` + Pages 线上复核 | `v1.1.0` tag |

---

## 八、决策记录（已于 2026-09-25 确认）

| # | 决策 | 结论 | 落地要点 |
|---|---|---|---|
| **D1** | `schemaVersion` 高于当前时 | ✅ **尽力导入** | 宽容解析，但**只写白名单内的已知字段**；未知字段在预览中列为「已忽略」，不写入 localStorage |
| **D2** | 备份提醒阈值 | ✅ **14 天** | 距上次导出 > 14 天 / 有改动未备份 / 从未备份 → 出现提示条，可「稍后再说」24 小时内不再出现 |
| **D3** | 单字段越界 | ✅ **逐字段警告 + 回退默认值** | 越界字段单独警告，其余可用数据照常导入 |
| **D4** | UI 层实现方 | ✅ **项目负责人直接实现** | 一次性完成数据安全卡片 / 预览对话框 / 提示条，不与 Trae 往返 |

> **⚠️ D1 的关键约束**：选「尽力导入」**不等于**「全量写入」。为避免把未知结构写进 `localStorage`，实现必须走**字段白名单**：
> 1. 只写入 `Config.KEYS` 中存在的字段；
> 2. 未知字段（含未来版本新增字段）→ 计入 `ignoredFields`，在导入预览中明示「已忽略」；
> 3. 这样既保留宽容性（能恢复多少恢复多少），又不会污染本地数据结构。

---

> 本文档已于 2026-09-25 确认，进入 S1 实施；实现完成后同步归档至项目资料库。
